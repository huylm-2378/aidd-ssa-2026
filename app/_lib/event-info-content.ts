/**
 * Hero event info block content (datetime / venue / broadcast note).
 * Ships with the MoMorph mock's literal rendered copy per the approved spec
 * (technical-spec.md > Assumptions), kept as named constants rather than
 * inline strings so they can be swapped later without touching markup.
 */
export const EVENT_DATETIME_LABEL = "26/12/2025";
export const EVENT_VENUE_LABEL = "Âu Cơ Art Center";
export const EVENT_BROADCAST_NOTE = "Tường thuật trực tiếp qua sóng Livestream";
